/** @type {import('next').NextConfig} */
const nextConfig = {
    images:{
        domains: ["images.unsplash.com","links.papareact.com","images.remotePatterns"],
    }
};

export default nextConfig;
